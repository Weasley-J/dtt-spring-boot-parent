package ${modulePackage}.mapper;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import ${domainFullyQualifiedClass};
import org.apache.ibatis.annotations.Mapper;

/**
 * The mybatis mapper interface of #if($domainDescription.length() > 0)${domainDescription}#else${modelEntityName}#end
 */
@Mapper
public interface ${domainSimpleClassName}Mapper extends BaseMapper<${modelEntityName}> {

}
